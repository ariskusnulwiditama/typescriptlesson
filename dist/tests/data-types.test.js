"use strict";
describe('Data Type', function () {
    it('must declare', function () {
        let name = "Joni";
        let balance = 90000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
