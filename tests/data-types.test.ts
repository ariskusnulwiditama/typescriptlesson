describe('Data Type', function() {
    it('must declare', function() {
        let name: string = "Joni";
        let balance: number = 90000;
        let isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
        
    });
});